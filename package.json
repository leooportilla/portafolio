{
  "name": "portafolio-com",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "rollup": "rollup --watch --config",
    "sass": "sass --watch ./src/styles/modules/style.scss ./src/styles/style.css"
  },
  "devDependencies": {
    "vite": "^5.2.0"
  },
  "dependencies": {
    "sass": "^1.77.2"
  }
}
